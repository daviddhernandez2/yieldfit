# URL del backend. En desarrollo apunta al Express local.
# En producción se define en el dashboard de Vercel apuntando al Render.
# Obligatoria: sin ella, `npm run dev` y `npm run build` fallan (ver vite.config.js).
VITE_API_URL=http://localhost:3000/api